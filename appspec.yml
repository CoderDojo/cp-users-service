version: 0.0
os: linux
files:
  - source: deploy/cp-users-service.conf
    destination: /etc/init/
  - source: deploy/cp-users-service.env
    destination: /etc/zen
  - source: /
    destination: /opt/zen/cp-users-service

permissions:
  - object: /opt/zen/cp-users-service
    owner: ubuntu
    group: ubuntu
  - object: /etc/zen/cp-users-service.env
    owner: ubuntu
    group: ubuntu
  
hooks:
  ApplicationStop:
    - location: deploy/stop-server.sh
      timeout: 300
      runas: root

  ApplicationStart:
    - location: deploy/start-server.sh
      timeout: 300
      runas: root

  

